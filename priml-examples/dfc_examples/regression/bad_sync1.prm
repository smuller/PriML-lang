priority High
priority Med
priority Low

order Low < Med
order Med < High

(* constraint violation {Med} <= {Low} *)
main {

    t <- spawn[Low] {
        ret 1
    };

    spawn[Med] {
        x <- sync t;
        ret x
    }
}
